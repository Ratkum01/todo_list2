name: todo_repository
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  todo_models:
    path: ../todo_models
  todo_services:
    path: ../todo_services
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
